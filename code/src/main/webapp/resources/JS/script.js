const constGp=document.querySelector('#mode');


function addConstraint() {
    // la fonction ajoute une contrainte a data via le controlleur qu'on connais par la vue
    // autrement dit on va dire au controlleur ce qu'il s'est passé (le user ajoute une contrainte)


}